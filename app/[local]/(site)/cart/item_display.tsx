import { Cart_Item } from "@/types/user";

export default function Item_Display({ brand, slug, price, quantity, images }: Cart_Item) {
    return <div className="flex justify-between items-center w-full gap-4 h-40 bg-secondary">
            
    </div>
}
